# Pinned to match the current imports in index.py / pages/*.py, which use
# the Dash 1.x style `import dash_core_components` / `import dash_html_components`
# (removed in Dash 2+). Use Python 3.8 or 3.9 for the smoothest install of
# this legacy set. See README for the modern-Dash alternative.
dash==1.20.0
dash-core-components==1.16.0
dash-html-components==1.1.3
dash-bootstrap-components==0.13.1
pandas>=1.3
numpy>=1.21
plotly>=5.0
gspread>=4.0
